# Deploy / production – subdomains on hhourssop.co.ke
# Use: docker compose --env-file .env.deploy up -d
# Ensure .env has real secrets (JWT_SECRET, ENCRYPTION_KEY, CLOUDINARY_*, DB password, etc.)

NODE_ENV=production

# Public URLs (must match DNS and reverse proxy)
NEXT_PUBLIC_API_URL=https://api.hhourssop.co.ke
NEXT_PUBLIC_SHOP_DOMAIN=shop.hhourssop.co.ke

# Admin is at https://admin.hhourssop.co.ke (same API URL)
# No extra var – admin uses NEXT_PUBLIC_API_URL

# API: allow admin and shop origins for CORS (comma-separated)
CORS_ORIGIN=https://admin.hhourssop.co.ke,https://shop.hhourssop.co.ke

# Media: use s3 (Spaces) in production or keep cloudinary
MEDIA_STORAGE=s3
# S3_* for DigitalOcean Spaces – set in .env

# Strong secrets in .env (not committed):
# JWT_SECRET=...
# JWT_REFRESH_SECRET=...
# ENCRYPTION_KEY=...
# DATABASE_URL=...  (if external DB)
# REDIS_URL=...     (if external Redis)
