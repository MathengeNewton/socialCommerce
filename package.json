{
  "name": "social-commerce-suite",
  "version": "1.0.0",
  "private": true,
  "description": "Social Media Publishing Platform + shop.domain Ecommerce",
  "packageManager": "pnpm@10.28.2",
  "scripts": {
    "dev": "pnpm --filter './apps/*' --filter './services/*' dev",
    "build": "pnpm --filter './apps/*' --filter './services/*' build",
    "migrate": "pnpm --filter api migrate",
    "seed": "pnpm --filter api seed"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
