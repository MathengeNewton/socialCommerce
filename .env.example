# =============================================================================
# hhourssop – environment template
# =============================================================================
# Copy this file to .env and fill in real values. Do not commit .env.
# For deploy URLs use: docker compose --env-file .env.deploy up -d
# (and keep .env for secrets; see DEPLOY.md)
# =============================================================================

# -----------------------------------------------------------------------------
# App mode
# -----------------------------------------------------------------------------
NODE_ENV=development

# -----------------------------------------------------------------------------
# Public URLs (used by admin and shop at build/runtime)
# Development:
#   NEXT_PUBLIC_API_URL=http://localhost:3004
#   NEXT_PUBLIC_SHOP_DOMAIN=localhost:3003
#   NEXT_PUBLIC_SHOP_URL=http://localhost:3003
# Production (example):
#   NEXT_PUBLIC_API_URL=https://api.hhourssop.co.ke
#   NEXT_PUBLIC_SHOP_DOMAIN=shop.hhourssop.co.ke
#   NEXT_PUBLIC_SHOP_URL=https://shop.hhourssop.co.ke
# -----------------------------------------------------------------------------
NEXT_PUBLIC_API_URL=http://localhost:3004
NEXT_PUBLIC_SHOP_DOMAIN=localhost:3003
NEXT_PUBLIC_SHOP_URL=http://localhost:3003

# -----------------------------------------------------------------------------
# Shop: tenant ID for the store catalog (UUID from Tenant table)
# -----------------------------------------------------------------------------
NEXT_PUBLIC_STORE_TENANT_ID=00000000-0000-0000-0000-000000000001

# -----------------------------------------------------------------------------
# API: CORS allowed origins (comma-separated)
# Development: http://localhost:3005,http://localhost:3003
# Production: https://admin.hhourssop.co.ke,https://shop.hhourssop.co.ke
# -----------------------------------------------------------------------------
CORS_ORIGIN=http://localhost:3005,http://localhost:3003

# -----------------------------------------------------------------------------
# API: Shop domain for link generation (product links in posts)
# Must match your shop domain (no protocol)
# -----------------------------------------------------------------------------
SHOP_DOMAIN=localhost:3003

# -----------------------------------------------------------------------------
# API: Auth and encryption (use strong random values in production)
# -----------------------------------------------------------------------------
JWT_SECRET=change-me-in-production
JWT_REFRESH_SECRET=change-me-in-production
ENCRYPTION_KEY=your-32-char-or-longer-secret-key

# -----------------------------------------------------------------------------
# Media: cloudinary (dev) or s3 (e.g. DigitalOcean Spaces)
# -----------------------------------------------------------------------------
MEDIA_STORAGE=cloudinary

# Cloudinary – https://console.cloudinary.com/
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret
CLOUDINARY_UPLOAD_FOLDER=hhourssop

# S3 / Spaces (when MEDIA_STORAGE=s3)
# S3_ENDPOINT=https://nyc3.digitaloceanspaces.com
# S3_ACCESS_KEY=your-access-key
# S3_SECRET_KEY=your-secret-key
# S3_BUCKET=your-bucket-name
# S3_REGION=nyc3

# -----------------------------------------------------------------------------
# OAuth – Facebook (for publishing to Facebook Pages / Instagram)
# API needs FACEBOOK_APP_ID + FACEBOOK_APP_SECRET
# Admin needs NEXT_PUBLIC_FACEBOOK_APP_ID for OAuth popup
# -----------------------------------------------------------------------------
# FACEBOOK_APP_ID=
# FACEBOOK_APP_SECRET=
# NEXT_PUBLIC_FACEBOOK_APP_ID=

# -----------------------------------------------------------------------------
# OAuth – TikTok (for publishing to TikTok)
# -----------------------------------------------------------------------------
# Admin needs NEXT_PUBLIC_TIKTOK_CLIENT_KEY for OAuth redirect
# TIKTOK_CLIENT_KEY=
# TIKTOK_CLIENT_SECRET=
# NEXT_PUBLIC_TIKTOK_CLIENT_KEY=

# -----------------------------------------------------------------------------
# Database and Redis (Docker Compose sets these by default; override for external)
# -----------------------------------------------------------------------------
# DATABASE_URL=postgresql://user:password@host:5432/dbname
# REDIS_URL=redis://host:6379
# REDIS_HOST=redis
# REDIS_PORT=6379

# -----------------------------------------------------------------------------
# Postgres (optional – Docker Compose defaults)
# -----------------------------------------------------------------------------
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=postgres
# POSTGRES_DB=social_commerce
